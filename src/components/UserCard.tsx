import React from "react"

type UserCardProps = {
    type : string,
}

export const UserCard = ({
    type,
}: UserCardProps) => {
  return (
    <div>
        UserCard
    </div>
  )
};

export default UserCard;
